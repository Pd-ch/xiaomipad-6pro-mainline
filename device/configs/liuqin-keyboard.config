# liuqin Nanosic WN8030 magnetic keyboard bridge.
# The driver must be built in: the persistent root intentionally carries no
# kernel module tree, and keyboard wake has to exist before the desktop starts.
CONFIG_HID_NANOSIC=y
CONFIG_HID_MULTITOUCH=y
