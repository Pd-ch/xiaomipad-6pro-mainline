[Unit]
# Requires + After is intentional.  Before alone only orders two jobs when
# both happen to start; it does not prevent NetworkManager from starting when
# the private factory identity is missing or invalid.
Requires=liuqin-wlan-mac.service
After=liuqin-wlan-mac.service
